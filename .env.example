# Optional — enable real contact form delivery via Formspree.
# Create a form at https://formspree.io and paste its ID (the part after /f/).
# Without this, the form falls back to opening the visitor's mail client (mailto).
NEXT_PUBLIC_FORMSPREE_ID=
